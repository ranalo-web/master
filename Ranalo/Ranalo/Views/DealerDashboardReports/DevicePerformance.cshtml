@model List<Ranalo.Models.DealerDeviceStock>
@{
    Layout = "~/Pages/Shared/_Layout.cshtml";
    const int pageSize = 20;
    const string key = "report-devices";
}

<link rel="stylesheet" href="~/css/watchlist-table.css?v=2">

<div class="nk-content-body">
    <div class="nk-block-head nk-block-head-sm">
        <div class="nk-block-between">
            <div class="nk-block-head-content">
                <h3 class="nk-block-title page-title">Device Performance</h3>
                <div class="nk-block-des text-soft"><p>Units financed, avg. value &amp; repayment health, by device model.</p></div>
            </div>
            <div class="nk-block-head-content">
                <a href="/@(ViewBag.BackLink)" class="btn btn-outline-light btn-white"><em class="icon ni ni-arrow-left"></em><span>Back to Dashboard</span></a>
            </div>
        </div>
    </div>

    <div class="nk-block">
        <div class="card card-bordered card-stretch">
            <div class="card-inner-group">
                <div class="card-inner p-0">
                    <div class="table-responsive">
                    <table class="table mb-0" style="white-space:nowrap;" data-key="@key" data-page-size="@pageSize">
                        <thead>
                            <tr>
                                <th class="wl-th" onclick="Ranalo.sortTable(this)">Device</th>
                                <th class="wl-th" onclick="Ranalo.sortTable(this)">Units</th>
                                <th class="wl-th" onclick="Ranalo.sortTable(this)">Avg Value</th>
                                <th class="wl-th" onclick="Ranalo.sortTable(this)">Good</th>
                                <th class="wl-th" onclick="Ranalo.sortTable(this)">Arrears</th>
                            </tr>
                        </thead>
                        <tbody>
                            @for (var i = 0; i < Model.Count; i++)
                            {
                                var d = Model[i];
                                <tr class="@(i >= pageSize ? "d-none" : "")" data-watchlist-row="@key">
                                    <td><span class="fw-medium">@d.Device</span></td>
                                    <td>@d.Units</td>
                                    <td>KES @d.AvgValue.ToString("N0")</td>
                                    <td><span class="text-success">@d.GoodPct.ToString("0")%</span></td>
                                    <td><span class="text-danger">@d.ArrearsPct.ToString("0")%</span></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    </div>
                    @if (Model.Count > pageSize)
                    {
                        <div class="card-inner d-flex" style="column-gap:1rem;">
                            <a href="#" class="link link-primary" data-more-for="@key" onclick="event.preventDefault(); Ranalo.showMoreRows(this, '@key', @pageSize);">Show @Math.Min(pageSize, Model.Count - pageSize) more</a>
                            <a href="#" class="link link-primary" data-less-for="@key" style="display:none;" onclick="event.preventDefault(); Ranalo.showLessRows(this, '@key', @pageSize);">Show less</a>
                        </div>
                    }
                    @if (Model.Count == 0)
                    {
                        <div class="card-inner text-soft">No device stock found.</div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/watchlist-table.js"></script>
}
